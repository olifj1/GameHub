# GameHub v1.7.1

- Colour Blocks now shifts whole rows and columns instead of reversing selected runs.
- Each swipe moves a row/column by one tile; the end tile wraps around to the opposite side while order is preserved.
- Puzzle generation now scrambles boards using the same wrapped shift moves, so every puzzle remains solvable.
- Roller Coaster remains parked: its files are preserved but it stays hidden from Home and Progress.
